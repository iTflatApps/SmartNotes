{
  "name": "parcel-start",
  "version": "1.0.0",
  "description": "Parcel start template",
  "browserslist": "> 0.5%, last 2 versions, not dead, not ie <= 11",
  "source": "src/index.html",
  "author": "Karvackyy Roma",
  "homepage": "https://github.com/Karvacky-Roma/gulp-build",
  "scripts": {
    "dev": "parcel",
    "build": "parcel build",
    "lighthouse": "lighthouse http://localhost:1234/index.html --output html --output-path ./report.html --view"
  },
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.0.0-rc.0",
    "@parcel/transformer-image": "^2.0.0-rc.0",
    "@parcel/transformer-webmanifest": "^2.0.0-rc.0",
    "parcel": "^2.0.0-rc.0",
    "posthtml-include": "^1.7.2"
  },
  "dependencies": {
    "focus-visible": "^5.2.0"
  }
}
